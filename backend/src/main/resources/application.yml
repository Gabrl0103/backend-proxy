server:
  port: ${PORT:8080}

spring:
  application:
    name: proxy-ai-platform

app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

logging:
  level:
    com.taller.proxy: DEBUG
